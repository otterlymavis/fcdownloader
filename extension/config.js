// Build-time configuration baked into this extension package.
// The packager may substitute the backend for another distribution, but the
// value is an internal implementation detail and is never user-configurable.

export const FCDL_DEFAULT_BACKEND = "https://fcdownloader-extractor.fly.dev";
export const FCDL_EXTENSION_BUILD = "dev";
export const FCDL_EXTENSION_BUILT_AT = "";
export const FCDL_MIN_HELPER_VERSION = "0.4.1-go";
